Memory in Python

how things are stored in memory
1 byte - 8 bits(its 0 or 1)
so 1 byte is 2 power 8
2 byte is 2 power 16

In C we define the types explictly int16, int32
Here 16 and 32 is bits - so its 2 power of x
But in python the data type is managed internally based on the size of the number
it automatically grows
Also the address we get id(variable) is not the actual address like in C
python only provides the address of the integer object not the actuall memory in the RAM
Python uses automatic memory management


C provides the actual memory in the RAM of the variable
The memory address in the RAM is provided as hexadecimal
ex: 0x7ffeefb08a2c
They uses hexa decimals beacuse the memory addresses are very very large numbers
Hex versions are shorter and easy to read

Why store things in RAM?
when a program or app runs the CPU has to read and write millions of records per seconds
so we store them in RAM(50ns) that has exceptional speed compared to disks even SSD(0.1ms)
also we cannot change the value in ROM in real time
only in computer memnory all the variables, arrays is stored when it is running and processing

Why memory represented in bytes(8bits)?
it is a hardware architecture universal standard that is defined and we use in all computer hardwares and systems
CPU designers standardized it to be 1 byte - the lowest memory unit
also 8 bits have some advantages - 256 numbers - fits in binary and hexadecimal well

what is provided as variable address?
even if a variable is 4 byts, 8 or 16 bytes we always get the starting location of the fisrt byte
this is very important to know - the programming language provides the location of the first byte of the variable stored
example if a variable is 2 bytes the RAM will stores it as 2 separate 8 bit bytes and provide the address of 1st byte
each memory address referes to exactly 1 byte in RAM

How program knows how many blocks or bytes to read for a variable?
We are already getting the starting byte address + we know the exact size of the variable int16 or int32 so the program knows
exactly how many bytes it should read from the memory

w3 schools explaination on memory in linkedin list chapter is amazing if someone can observe the details - simple and detailed!!

Why array indexes starts at 0?
array indexes are simple pointer arithmetic - *(arr + 0) like base + 0 * size of element
so in order to keep things easier the array index is starting at 0 if it starts with 0 the logic beacmes extra, slower and its unessary
arr[i] → base_address + (i × bytes_per_element)
short answers - it makes it easier to do address(pointer) calculation having 0 as starting. Also maps exactly how RAM works in storing and retriving data
it started from C and all languages copied it

Statement and Explaination Learned:
Statement: This microcontroller has an 8 bit architecture with 16 bit address bus and uses two bytes for integers and two bytes for memory addresses
[Ref: from w3 schools]

Explaination:
1. 8 bit architure basically means a system processes 8 bit(1 byte) at a time - thats how its designed
2. so it can process the range 0-255 or -128 to +127 at a time. it can also process bigger numbers but it will take more time as there will be more steps like carry and other steps - make it slower
3. memory bus - we have RAM that has locations to store data. We need to get the memory location no this is what address bus does - it is like a wire, signal that carries the memory no
4. address bus size - how many address it can create in the CPU - ex 16 bit address bus size can create 2^16 = 65,536 address locations it can create
if address bus is N bits it can create 2^n bits
now usually 1 address will be 1 byte - so 65,536 bytes will be 64KB memory it can reach
If a computer has 16 GB RAM, it needs enough address bus bits to “label” every byte in that 16 GB
basically rememebr - no of address it can create to hold the data

Variables - data types size and range
int16 - 2^16 - it can hold ranges from 0 to 65535 or the positive to negative range
a 8 bit CPU can process 16bit values but it processes everythign in 8 bits , so takes multiple steps that will be slower

Architecture CPU standard in 2026
64bit is the standard for CPUs in our age - it process all the data in 64bits in one instructions! great
Addressing - same as we saw before it says how many addresses it can point to
it seems a modern 64bit systems can have huge RAMs but we might have seen there will be a limit for RAM mentioned this is specifically due to OS we have or the motherboard we have
(example - the thinkpad laptop i have it is limited to 16GB ram based on its motherboard)
for micro controllers 32bit is the most common standard - 64bit in this case are rare gpt said














